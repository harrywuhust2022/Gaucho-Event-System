<h1>User lists</h1>
<ul>
    <% @users.each do |user| %>
        <li>
            <%= link_to user.username, user %><br>
            <%= user.email %><br>
            
        </li>
    <% end %>
</ul>
<%= paginate @users %>
<%= link_to "new user", new_user_path%>